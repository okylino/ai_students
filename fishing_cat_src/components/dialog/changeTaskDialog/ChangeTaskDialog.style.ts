import styled from 'styled-components';

import { BaseTitle } from '@fishing_cat/components/dialog/basicDialog/BasicDialog.style';

export const Title = styled(BaseTitle)`
  font-size: 20px;
  margin-bottom: 24px;
`;

export default Title;
