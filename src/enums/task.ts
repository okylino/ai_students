export enum TASK_TYPE {
  SCREENSHOT = 'SCREENSHOT',
  LINK = 'LINK',
}

export enum TASK_RESULT {
  MARKED = 'Marked',
  SUBMITTED = 'Submitted',
  NOT_SUBMITTED = 'Not submitted',
}

export enum LINK_RESULT {
  OPENED = 'Opened',
  NOT_OPEN = 'Not Open',
}
